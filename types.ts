export interface Skill {
  name: string;
  slug: string;
  color: string;
  iconUrl?: string;
}

export interface SocialLink {
  name: string;
  url: string;
  icon: string;
  color: string;
}

export interface WaifuImage {
  url: string;
  alt: string;
}
